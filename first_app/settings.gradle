import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    // Here you have to change RepositoriesMode.FAIL_ON_PROJECT_REPOS to RepositoriesMode.PREFER_PROJECT
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) // made changes here
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "first_app"
include ':app'
setBinding(new Binding([gradle: this]))                                // new
evaluate(new File(                                                     // new
        settingsDir.parentFile,                                              // new
        'first_module/.android/include_flutter.groovy'                         // new
))
